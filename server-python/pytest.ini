[pytest]
testpaths = tests
# tests/ first so test modules can import helpers next to them; then the project
# root so `from app...` resolves. tests/conftest.py sets the env vars app.config
# and app.db require at import time.
pythonpath = tests .
addopts = -q
